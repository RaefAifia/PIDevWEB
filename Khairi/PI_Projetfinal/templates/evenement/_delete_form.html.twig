<form method="post" action="{{ path('evenement_delete', {'evenement_id': evenement.evenement_id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evenement.evenement_id) }}">
    <button class="btn">Delete</button>
</form>
