{% extends 'dashboard.html.twig' %}

{% block title %}LieuEvenement{% endblock %}

{% block body %}
    <h1>LieuEvenement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lieu_evenement.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ lieu_evenement.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ lieu_evenement.description }}</td>
            </tr>
            <tr>
                <th>Longitude</th>
                <td>{{ lieu_evenement.longitude }}</td>
            </tr>
            <tr>
                <th>Latitude</th>
                <td>{{ lieu_evenement.latitude }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('lieu_evenement_index') }}">back to list</a>

    <a href="{{ path('lieu_evenement_edit', {'id': lieu_evenement.id}) }}">edit</a>

    {{ include('lieu_evenement/_delete_form.html.twig') }}
{% endblock %}
