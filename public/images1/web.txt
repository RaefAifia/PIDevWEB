 composer: gestion des pquets externes
flex: plugin composer ppour automatisation de package bundle facilite aijout/supr des dependaces
pour creer squelette de notre projet

1 action : fct public 
2 @route (lien entre action presente dans controller et URL)
3 la reponse 


internet est un rx informatique dont le web est un service parmi autres
framework: infra logicielle fournissant des templates/des services/design

architecture (client/serveur) est n-tiers ou 2-tiers(car a besoin de 2 emplacements de machines)
archi 3-tiers(symphony): client <=>SRV application <=> SRV BD: high security level/ partage des taches assure la rapidité des reponses

html/css/js => statique : si je veux changer qlq chose je dois passer par le code
js: permet controle de saisi  et l'interactivité avec le client
php=> dynamique : si je veux changer qlq chose sans passer par le code est c'est à travers la BD
aspect visuel d'un site:
frontoffice: definie ce que l'internaute voit en naviguant sur un site internet 
backoffice: designe ce que l'admin voit voit suite à une cnx à son espace admin

aspect code:
frontend: les maquettes avec photoshop ou...+ framework front
backend: concept serveur+ hebergement web+ BD

php:
public function _construct(String $prenom, String $nom){
$this->setPrenom($prenom);
$this->setNom($nom);
}
il faut garder un seul au niveau d'une classe 

html:
<script\> appel code js
<link\> appel fichier css
meta:  standardisation de fichier html



framework: cadre de travail offrant des outils (des classes/interfaces) sous fomes des bib 
offre la  squelette d'une application 

déroulement d'une app symfony:
client envoie une requete http 
chaque requete va executer el front controller(point d'entree ou racine)	
front controller: comm directe avec noyau symfony via routing (requete url)
puis routing indique des info sur le controller cad la methode
et le controller va renvoyer réponse vers le noyau et ce noyau va chercher et donner l'action à executer cette methode =>on aura une page web 

composer create project symfony/website-skeleton projectname "4.4 " //je vex creer un projet siteweb alors telecharge moi toutes les dependances necessaires
composer require symfony/web-server bundle // installer paquet serveur

repository: sert à recuperer des donnes des entités à travers des methodes(find()/findall()..)



composer create-project symfony/website-skeleton PIwebXcode "4.4.*"


composer require symfony/web-server-bundle


php bin/console doctrine:mapping:import "App\Entity" annotation --path=src/Entity

(repositoryClass="App\Repository\MyClassRepository")
php bin\console make:entity --regenerate