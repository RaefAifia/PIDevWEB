{% extends 'baseAdmin.html.twig' %}
{% block title %}Cours{% endblock %}

{% block body %}

    <h1>Cours</h1>

    <div class="row" >
         <div class="col-sm-6 col-lg-4" align-self-center>

            <th></th>
            <img src="/images1/{{ cour.image }}" class="img-fluid"  alt="" style="width: 50%; height:auto; margin:50px">
        </div>

        <div class="col-sm-6 col-lg-4" align-self-center>
            <table class="table">
                <tbody>

                <tr>
                    <th>Titre</th>
                    <td>{{ cour.titre }}</td>
                </tr>
                <tr>
                    <th>Niveau</th>
                    <td>{{ cour.niveau }}</td>
                </tr>
                <tr>
                    <th>Description</th>
                    <td>{{ cour.description }}</td>
                </tr>
                <tr>
                    <th>Duree</th>
                    <td>{{ cour.duree }}</td>
                </tr>
                <tr>
                    <th>
                        <a href="{{ asset('images1/' ~ cour.image) }}">Voir le document associé </a>
                    </th>
                </tr>
                </tbody>
            </table>
        </div>
    </div>

    {#  .center {
    margin-left: auto;
    margin-right: auto;
    } <a href="{{ path('cours_index') }}">back to list</a> <a href="{{ path('cours_edit', {'coursId': cour.coursId}) }}">edit</a>
    {{ include('cours/_delete_form.html.twig') }}#}






{% endblock %}