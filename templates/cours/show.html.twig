{% extends 'base.html.twig' %}

{% block title %}Cours{% endblock %}

{% block body %}
    .center {
    margin-left: auto;
    margin-right: auto;
    }
    <h1>Cours</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>CoursId</th>
                <td>{{ cour.coursId }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ cour.titre }}</td>
            </tr>
            <tr>
                <th>Niveau</th>
                <td>{{ cour.niveau }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ cour.description }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ cour.duree }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ cour.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cours_index') }}">back to list</a>

    <a href="{{ path('cours_edit', {'coursId': cour.coursId}) }}">edit</a>

    <a href="{{ asset('images1/' ~ cour.image) }}">View image </a>

    {{ include('cours/_delete_form.html.twig') }}
{% endblock %}
