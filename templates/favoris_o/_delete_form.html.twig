<form method="post" action="{{ path('favoris_o_delete', {'favorisOId': favoris_o.favorisOId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ favoris_o.favorisOId) }}">
    <button class="btn">Delete</button>
</form>
