<form method="post" action="{{ path('formation_delete', {'formationId': formation.formationId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formation.formationId) }}">
    <button class="btn_1">Delete</button>
</form>
