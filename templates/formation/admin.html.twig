{% extends 'base.html.twig' %}

{% block title %}Formation index{% endblock %}

{% block body %}
<h1>Formation index</h1>
<section id="team" class="team section-bg">
    {#  <div class="container" data-aos="fade-up">#}

    <div class="section-title">
        <h2>Team</h2>
        <p>Les formations </p>
    </div>
    {<table class="table">
        <thead>
        <tr>
            <th>CoursId</th>
            <th>Titre</th>
            <th>Niveau</th>
            <th>Description</th>
            <th>Duree</th>
            <th>Image</th>
            <th>actions</th>
        </tr>
        </thead>
        <tbody>
        {% for formation in formations %}
            <tr>

                <td>{{ formation.titre }}</td>
                <td>{{ formation.niveau }}</td>
                <td>{{ formation.description }}</td>
                <td>{{ formation.duree }}</td>
                <td>{{ formation.image }}</td>
                <td>
                    <a href="{{ path('cours_show', {'coursId': cour.coursId}) }}">show</a>
                    <a href="{{ path('cours_edit', {'coursId': cour.coursId}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
            <a href="{{ path('formation_show', {'formationId': formation.formationId}) }}">show</a>
            <a href="{{ path('formation_edit', {'formationId': formation.formationId}) }}">edit</a>
        </div>
        </div>
        </div>


</section>
    <a href="{{ path('nonValid') }}">Afficher non validees</a>
    <a href="{{ path('Valid') }}">validees</a>
    <a href="{{ path('formation_index') }}">toutes les formations</a>
{% endblock %}