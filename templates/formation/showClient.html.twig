{% extends 'base.html.twig' %}

{% block title %}Formation index{% endblock %}
   {% block formationIntro %}

       <section class="breadcrumb breadcrumb_bg">
           <div class="container">
               <div class="row">
                   <div class="col-lg-12">
                       <div class="breadcrumb_iner text-center">
                           <div class="breadcrumb_iner_item">
                               <h2>Espace Formation</h2>
                               <p>Home<span>/</span>Courses</p>
                           </div>
                       </div>
                   </div>
               </div>
           </div>
       </section>
   {% endblock %}
{% block body %}
    {# ena zedt  section okhra  #}
    <section class="special_cource padding_top">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-xl-5">
                    <div class="section_tittle text-center">

                        <h2>Les formations Artistiques</h2>
                        <a class="btn_1" href="{{ path('formation_new') }}">Ajouter Formation?</a>
                    </div>
                </div>

            </div>



        </div>



        <div class="row">
            {% for formation in formations %}
                <div class="col-sm-6 col-lg-4">
                    <div class="single_special_cource">
                        <img src="/images1/{{ formation.image }}" class="img-fluid" alt="" style="width: 50%; height:auto">
                        <div class="special_cource_text">


                            {# <a href="course-details.html" class="btn_4">Web Development</a> ken nheb naamel click aala esm formation#}

                            <h2>{{ formation.titre}}</h2>
                            <h4 class="text-primary">{{ formation.prix}} DT </h4>

                            <h5>{{ formation.description  }}</h5>


                            <div class="author_info">
                                <div class="author_img">
                                    <a href="{{ path('formation_show', {'formationId': formation.formationId}) }}" class="btn_4">Afficher</a>

                                </div>

                            </div>
                        </div>

                    </div>
                </div>
            {% endfor %}

        </div>
        </div>

    </section>
    {#   {% if oeuvrage.quantite==0 %}
                                    <span class="label-danger ">hors stock</span>
                                {% else %}
                                    <h4> {{ oeuvrage.quantite }} </h4>
                                {% endif %}  #}

    {#   {% else %}




{% endblock %}
