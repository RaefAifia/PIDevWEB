<form method="post" action="{{ path('inscription_delete', {'inscriptionId': inscription.inscriptionId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ inscription.inscriptionId) }}">
    <button class="btn">Delete</button>
</form>
