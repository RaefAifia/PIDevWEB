<form method="post" action="{{ path('oeuvrage_delete', {'oeuvrageId': oeuvrage.oeuvrageId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ oeuvrage.oeuvrageId) }}">
    <button class="btn">Delete</button>
</form>
