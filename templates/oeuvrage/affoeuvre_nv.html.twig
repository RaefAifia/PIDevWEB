{% extends 'baseBack.html.twig' %}

{% block title %}
    <title> liste des oeuvres </title>
{% endblock %}

 {% block body %}
     <div class="main">
         <!-- MAIN CONTENT -->
         <div class="main-content">
             <div class="container-fluid">
                 <!-- OVERVIEW -->
                 <div class="panel panel-headline">
                     <div class="panel-heading">
                         <h3 class="panel-title">Liste des oeuvres</h3>
                         <p class="panel-subtitle"></p>
                     </div>
                     <!-- changer lllllllllll -->


                     <table class="table">
                         <thead>
                         <tr>

                             <th>Nom</th>
                             <th>Image</th>
                             <th>Description</th>
                             <th>prix</th>

                             <th>Quantité</th>
                             <th>actions</th>
                         </tr>
                         </thead>
                         <tbody>
                         {% for oeuvre in oeuvrages %}
                             <tr>

                                 <td>{{ oeuvre.nom }}</td>
                                 <td>{{ oeuvre.image }}</td>
                                 <td>{{ oeuvre.description }}</td>
                                 <td>{{ oeuvre.prix }}</td>
                                 <td>{{ oeuvre.quantite }}</td>



                                 <td>
                                     <a href="{{ path('oeuvrage_show', {'oeuvrageId': oeuvre.oeuvrageId}) }}" class="btn_4" >Afficher </a>
            {% if oeuvre.isvalid == 0 %}
        <a href="{{ path('oeuvrage_show', {'oeuvrageId': oeuvre.oeuvrageId}) }}" class="btn_4" > Valider </a>

        {% endif %}
                                 </td>
                             </tr>

                         {% endfor %}
                         </tbody>
                     </table>
                     <div class="panel-body">

                     </div></div>
                 <!-- END OVERVIEW -->

             </div>
         </div>
         <!-- END MAIN CONTENT -->
     </div>
 {% endblock %}