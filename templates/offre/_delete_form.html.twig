<form method="post" action="{{ path('offre_delete', {'offreId': offre.offreId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offre.offreId) }}">
    <button class="btn btn-danger" style="float: right">Supprimer</button>
</form>
