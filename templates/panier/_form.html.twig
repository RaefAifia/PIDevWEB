{{ form_start(form) }}
    {{ form_widget(form) }}
<center><button style="margin-top: 30px; margin-bottom: 30px" class="btn_2">{{ button_label|default('Confirmer La Commande') }}</button></center>
{{ form_end(form) }}
