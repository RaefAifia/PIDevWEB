{% extends 'base.html.twig' %}

{% block body %}
    <section class="banner_part">
        <div class="container">



                            <h2>Liste réclamation </h2>

                            <table class="table">
                                <thead>
                                <tr>

                                    <th>Nom de reclamation</th>
                                    <th>Description</th>
                                    <th>Date</th>

                                    <th>actions</th>
                                </tr>
                                </thead>
                                <tbody>
                                {% for reclamation in reclamations %}
                                    <tr>

                                        <td>{{ reclamation.reclamationNom }}</td>
                                        <td>{{ reclamation.description }}</td>
                                        <td>{{ reclamation.date ? reclamation.date|date('Y-m-d') : '' }}</td>

                                        <td>
                                            <a href="{{ path('reclamation_show', {'reclamationId': reclamation.reclamationId}) }}">show</a>
                                            <a href="{{ path('reclamation_edit', {'reclamationId': reclamation.reclamationId}) }}">edit</a>
                                        </td>
                                    </tr>
                                {% else %}
                                    <tr>
                                        <td colspan="6">no records found</td>
                                    </tr>
                                {% endfor %}
                                </tbody>
                            </table>

                            <a href="{{ path('reclamation_new') }}">Create new</a>
                        </div>


    </section>
{% endblock %}

