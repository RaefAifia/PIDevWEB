{% extends 'base.html.twig' %}

{% block body %}
    <section class="banner_part">
        <div class="container">



                            <h2>Reclamation</h2>

                            <table class="table">
                                <tbody>

                                <tr>
                                    <th>Nom de réclamation</th>
                                    <td>{{ reclamation.reclamationNom }}</td>
                                </tr>
                                <tr>
                                    <th>Description</th>
                                    <td>{{ reclamation.description }}</td>
                                </tr>
                                <tr>
                                    <th>Date</th>
                                    <td>{{ reclamation.date ? reclamation.date|date('Y-m-d') : '' }}</td>
                                </tr>

                                <tr>
                                    <th>Concernant</th>
                                    <td>{{ reclamation.x }}</td><td>{{ reclamation.concernant }}</td>
                                </tr>

                                </tbody>
                            </table>

            <button class="btn float-right" onclick="location.href='{{ path('reclamation_edit', {'reclamationId': reclamation.reclamationId}) }}'">Edit</button>
            {{ include('reclamation/_delete_form.html.twig') }}
            <a href="{{ path('reclamation_index') }}">back to list</a>



        </div>
    </section>
{% endblock %}
