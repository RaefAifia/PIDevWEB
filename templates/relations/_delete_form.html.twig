<form method="post" action="{{ path('relations_delete', {'follower': relation.follower}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ relation.follower) }}">
    <button class="btn">Delete</button>
</form>
