{% extends 'base.html.twig' %}



{% block body %}
<section class="banner_part">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col align-self-center " >

                <img src="\images\profil\{{ user.image }}" class="img-fluid" alt="" style="width: 50%; height:auto">
            </div>
            <div class="col-sm-8">
                <div class="banner_text">
                    <div class="banner_text_iner">
    <h1>Profil utilisateur</h1>

    <table class="table">
        <tbody>

            <tr>
                <th>Nom</th>
                <td>{{ user.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ user.prenom }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ user.username }}</td>
            </tr>

            <tr>
                <th>Adresse</th>
                <td>{{ user.adresse }}</td>
            </tr>

            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>

            <tr>
                <th>Bio</th>
                <td>{{ user.bio }}</td>
            </tr>

        </tbody>

    </table>

    {% if(user.userId != app.user.userId) %}
        <a href="{{ path('user_index') }}">back to list</a>
                    </br>
        {% if relation==null %}

        <a href="{{ path('user_sabonner' , {'userId': user.userId}) }}">S'abonner</a>
        {% else %}
            <a href="{{ path('user_sabonner' , {'userId': user.userId}) }}">Se désabonner</a>
        {% endif%}
            {% else %}
        {{ include('user/_delete_form.html.twig') }}

    {% endif%}


    <!--<a href="{{ path('user_edit', {'userId': user.userId}) }}">edit</a>-->


                    </div>
                </div>
            </div>



            </div>
        </div>
    </div>
</section>
{% endblock %}
